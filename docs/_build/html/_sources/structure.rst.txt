Repository structure
====================

The authoritative file-by-file map of the repository lives in
``structure.md`` at the repo root (per AGENTS.md §4 rule 10). It is
included verbatim below so it stays the single source of truth.

.. include:: ../structure.md
   :parser: myst_parser.sphinx_
