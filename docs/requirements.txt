# Documentation build dependencies for the GitHub-Pages workflow.
# Keep this minimal: heavy package deps are mocked via
# ``autodoc_mock_imports`` in docs/conf.py.
sphinx>=7.3,<9
furo>=2024.5.6
myst-parser>=2.0,<4
sphinx-copybutton>=0.5.2
