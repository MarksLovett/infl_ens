Getting started
===============

The page below is rendered from the repository ``README.md`` via
:mod:`myst_parser`. Edit ``README.md`` at the repo root to change it; the
GitHub-Pages build will pick the new content up on the next push to
``main``.

.. include:: ../README.md
   :parser: myst_parser.sphinx_
